import {createTheme} from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#4CAF50',
    },
    secondary: {
      main: '#ffffff',
    },
    background: {
      default: '#003145',
    },
  },
  typography: {
    fontFamily: '"Poppins", sans-serif',
  },
})
